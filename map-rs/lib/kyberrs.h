#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
void maptopointg2(char *msg, size_t msg_len,
                  char *point, 
                  char *domain, size_t domainLen);
void maptopointg1(char *msg, size_t msg_len,
                  char *point, 
                  char *domain, size_t domainLen);
